server {
    listen 80;
    server_name _;

    # Redirect HTTP to HTTPS in production
    # Uncomment after SSL cert is set up via AWS ACM / Certbot
    # return 301 https://$host$request_uri;

    location / {
        proxy_pass http://127.0.0.1:8000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_read_timeout 60s;
        client_max_body_size 10M;
    }
}

# HTTPS block — enable after SSL setup
# server {
#     listen 443 ssl;
#     server_name healthmax.health;
#     ssl_certificate /etc/ssl/certs/healthmax.crt;
#     ssl_certificate_key /etc/ssl/private/healthmax.key;
#     location / {
#         proxy_pass http://127.0.0.1:8000;
#         ...
#     }
# }
